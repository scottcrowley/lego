<?php

namespace App\Console\Commands;

use Illuminate\Console\Command;

class CheckSetsCsv extends Command
{
    use CommandHelpers, CsvHelpers, CheckHelpers;

    /**
     * The name and signature of the console command.
     *
     * @var string
     */
    protected $signature = 'lego:check-sets-csv
                            {--bulk : Command being run with other commands}';

    /**
     * The console command description.
     *
     * @var string
     */
    protected $description = 'Verifies that all table dependencies are present from csv file generated by Rebrickable.';

    /**
     * CSV File Url
     *
     * @var string
     */
    protected $url = 'https://cdn.rebrickable.com/media/downloads/sets.csv.gz';

    /**
     * Create a new command instance.
     *
     * @return void
     */
    public function __construct()
    {
        parent::__construct();

        $this->checkTables = [
            'themes' => ['id', 'theme_id']
        ];

        $this->missingDeps = $this->loadedDeps = [
            'themes' => []
        ];

        $this->keys = collect([
            'set_num',
            'name',
            'year',
            'theme_id',
            'num_parts'
        ]);
    }

    /**
     * Display command details and request confirmation to continue
     *
     * @return bool
     */
    protected function start()
    {
        $this->info('>> Please wait while we verify all the dependencies are available for Sets <<');
        return true;
    }
}
